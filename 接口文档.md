# 文字驱动语音 · 对外接口文档

> 本文档给**对接方**（其他项目/系统）使用：服务启动后接口即生效，按本文档直接调用即可。
> 文档随项目分发；如接口有变更会同步更新本文件。
> 服务提供「查状态 → 查角色 → 配音」三个接口，都基于 HTTP/JSON，无鉴权、无 Cookie。

## 0. 服务地址

| 项 | 说明 |
| --- | --- |
| 服务地址 | `http://<服务机IP>:18062`（本机调用用 `http://127.0.0.1:18062`） |
| 端口 | **固定 18062**，对外地址稳定不变（本项目不会自动换端口）。对接方把地址写死即可，无需探测端口或读端口文件 |
| 前提 | **服务已启动**（服务方双击 `start.bat` 启动）。未启动时所有接口连不上/超时，即"接口返回异常 = 服务没启动" |

> 下文示例统一用 `http://127.0.0.1:18062`，对接时替换成实际地址。

### 0.1 为什么固定 18062（不再漂移）

- **历史问题**：本项目早期用 8060，并带"端口被占就自动往下挑"的漂移逻辑。结果是**对外地址每次可能不一样**，对接方要么猜、要么读 `tmp\port.txt`，非常脆。
- **为什么不用 80xx**：Windows 的**动态端口范围**（系统随机分配给出站连接的端口池）在本机是 **1024~15000**（被 Docker/WSL 改过，Windows 默认是 49152 起）。80xx 落在池子里，会被系统临时连接**随机抢占**，这才是"端口莫名被占用"的真正原因。
- **为什么选 18062**：`18062 > 15000`，**天然在动态端口池之外**，不会被系统临时连接抢占；同机的 `duihuamoxing` 用 8061/18060，端口完全错开。
- **现在的策略**：端口**写死 18062，永不漂移**。`start.bat` 不再扫描端口、不再读/写 `tmp\port.txt`（该文件已删除）；`choose_port.ps1` 已移除。

### 0.2 端口能不能改（TTS_API_PORT）

**能，但只在"部署到新机器、18062 确实被别的程序长期占用"时才改。**

```bat
rem Windows：启动前设环境变量
set TTS_API_PORT=19062
start.bat
```

```bash
# Linux/macOS
TTS_API_PORT=19062 ./start.sh
```

优先级：`环境变量 TTS_API_PORT` > 代码里的默认值 `18062`。

> 改端口后**所有对接方都要跟着改**。所以默认 18062 不要动——本文档承诺的就是这个地址。

### 0.3 当前对接方（消费本服务的项目）

| 项目 | 路径 | 配置位置 |
| --- | --- | --- |
| easy-story | `D:\xm\easy-story` | `config.json` 的 `tts.baseUrl` |
| FunnyAnimationAssitant | `D:\xm\FunnyAnimationAssitant` | `tools\lib-ttsapi.mjs` 的 `BASE`（可用 `TTS_API_BASE` 覆盖） |
| zhuomianchunwu / airi | `D:\xm\zhuomianchunwu\airi` | `packages\stage-ui\src\stores\providers.ts` 的 `defaultBaseUrl`（走 vite 代理） |

> 注意：`duihuamoxing` 用的是**它自己那一份** TTS 服务副本（8061 / 18060），**不消费本服务**，不受影响。

---

## 对接流程总览（推荐）

```
1. GET  /health            → 服务是否就绪（200 且 status=ok 才继续；连不上 = 未启动/未就绪）
2. GET  /models            → 拿角色列表，取 ready=true 的角色名作为角色 ID
3. POST /v1/audio/speech   → 传 {角色ID, 文本}，响应体就是音频文件（mp3），保存即可
```

角色列表**不用缓存**：模型目录热加载，服务方放入新角色后下一次 `GET /models` 就能看到。

---

## 接口 1：查询服务状态

### `GET /health`

判断服务是否已启动、是否就绪。**HTTP 200 且 `status == "ok"` 才算正常**；连接被拒、超时、非 200 都视为"服务不可用"，由对接方决定如何提示。

请求示例：

```bash
curl http://127.0.0.1:18062/health
```

响应示例（200）：

```json
{
  "status": "ok",
  "service": "wenziqudong-tts",
  "port": 18062,
  "device": "cuda:0",
  "infer_device": "cuda",
  "ready_roles": ["azhong", "liejun"],
  "max_chars": 1000,
  "sample_steps": 64
}
```

| 字段 | 说明 |
| --- | --- |
| `status` | `"ok"` = 服务正常 |
| `ready_roles` | 当前可直接合成的角色名列表（同接口 2） |
| `max_chars` | 单次合成字数上限（默认 1000），长文本请按此分段 |
| `port` | 实际监听端口（固定 18062） |

> 服务刚启动的头几分钟（首次加载约 5–10 分钟）`/health` 不通属正常，稍后重试即可。

---

## 接口 2：查询声音角色列表

### `GET /models`

返回全部角色及可用状态。**角色 ID = `name` 字段**（即模型目录名），只应使用 `ready == true` 的角色。

请求示例：

```bash
curl http://127.0.0.1:18062/models
```

响应示例（200）：

```json
{
  "models": [
    { "name": "azhong",     "ready": true,  "missing": [] },
    { "name": "fengyanlin", "ready": true,  "missing": [] },
    { "name": "someone",    "ready": false, "missing": ["ref.wav", "ref_text.txt"] }
  ]
}
```

| 字段 | 说明 |
| --- | --- |
| `name` | 角色 ID（配音时传这个） |
| `ready` | `true` = 4 件套齐全、可直接合成 |
| `missing` | `ready=false` 时缺哪些文件（对接方无需处理，服务方补文件即可） |

### 兼容格式：`GET /v1/audio/voices`（OpenAI 风格，只返回可用角色）

```json
{ "voices": [ { "id": "azhong", "name": "azhong" }, { "id": "liejun", "name": "liejun" } ] }
```

---

## 接口 3：配音（角色 ID + 文本 → 语音文件）

### `POST /v1/audio/speech`（推荐，JSON 请求，返回 mp3）

请求体 JSON：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `voice` | string | 是 | 角色 ID（接口 2 里的 `name`；也兼容写 `character` 字段） |
| `input` | string | 是 | 要合成的文本（也兼容写 `text` 字段）；≤1000 字，超长会被拒（400） |
| `speed` | float | 否 | 语速，默认 1.0；范围 0.1~3.0，建议 0.6~1.65 |
| `model` | string | 否 | 同 `voice`（OpenAI 兼容别名，二者传其一即可） |

响应：**音频文件二进制**（不是 JSON）——正常为 `audio/mpeg`（mp3）；服务机缺 ffmpeg 时回退 `audio/wav`（以响应头 `Content-Type` 为准）。把响应体直接写成 `.mp3` 文件即可播放。

请求示例：

```bash
curl -X POST http://127.0.0.1:18062/v1/audio/speech \
  -H "Content-Type: application/json" \
  -d "{\"voice\":\"azhong\", \"input\":\"大家好，这是一段测试配音。\", \"speed\":1.0}" \
  --output dub.mp3
```

```python
import requests

BASE = "http://127.0.0.1:18062"

def dub(voice_id: str, text: str, speed: float = 1.0) -> bytes:
    r = requests.post(f"{BASE}/v1/audio/speech",
                      json={"voice": voice_id, "input": text, "speed": speed},
                      timeout=300)
    r.raise_for_status()
    return r.content          # mp3 二进制，直接写文件或交给播放器

with open("dub.mp3", "wb") as f:
    f.write(dub("azhong", "大家好，这是一段测试配音。"))
```

```javascript
const r = await fetch("http://127.0.0.1:18062/v1/audio/speech", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ voice: "azhong", input: "大家好，这是一段测试配音。" }),
});
if (!r.ok) throw new Error((await r.json().catch(() => ({}))).detail || r.statusText);
const blob = await r.blob();          // audio/mpeg，可直接 new Audio(URL.createObjectURL(blob)).play()
```

### 替代：`POST /tts`（表单请求，返回 wav）

功能相同，请求为 `multipart/form-data`（传统表单上传），返回 **wav**（32kHz），适合不方便发 JSON 的调用方。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `character` | string | 是 | 角色 ID |
| `text` | string | 是 | 要合成的文本，≤1000 字 |
| `speed` | float | 否 | 语速，默认 1.0 |

```bash
curl -X POST http://127.0.0.1:18062/tts \
  -F "character=azhong" -F "text=大家好，这是一段测试配音。" \
  --output dub.wav
```

---

## 🔊 角色切换与耗时（重要，对接方须知）

服务用 GPT-SoVITS 音色模型合成语音。**显存放不下所有角色模型**，因此服务端会自动管理模型的加载与卸载——对接方**不需要也不应该**做任何模型管理，只管在配音请求里传角色 ID，其余全是服务的事。

### 调用方视角的耗时（实测口径）

| 场景 | 实测耗时 | 说明 |
| --- | --- | --- |
| 同角色连续调用 | 约 1 秒级 | 角色模型已在显存中，最快 |
| **换角色调用** | 约 1~3 秒 | 服务自动卸载上一个角色、加载新角色，调用方无需任何处理，只是这次请求稍慢 |
| 服务启动后的头几个请求 | 5~15 秒 | 引擎/文本前端一次性初始化，之后不再出现 |
| 长文本（数百~1000 字） | 数十秒 | 合成本身与文本长度成正比 |

> **来回切换 A→B→A 属正常现象**：默认模式下显存只保留当前角色，切回 A 时会重新加载，每次切换都有 1~3 秒开销——这是设计行为，不是故障，不要据此做服务异常判断。

### 并发与排队

- 服务**同一时刻只合成一条**语音，并发请求自动排队依次处理：不会出错，但**等待时间会累加**（排在前面的每条都要等）。
- 高并发场景请在对接方侧做排队/去重，不要同时发起大量配音请求。

### 对接方建议

1. 配音请求超时设 **≥ 300 秒**（覆盖长文本合成 + 并发排队等待），不要因单次变慢就判定失败；
2. 判断服务是否可用，一律以 `GET /health`（接口 1）为准，不要以某次配音的快慢来判断；
3. 偶发 500 可原样重试一次；
4. 如果业务上固定常用少数几个角色、希望"切换零开销"，可请**服务方**开启多角色缓存模式（用过的角色常驻显存、切换秒回；代价是每个角色多占约 230MB 显存）——这是服务端配置，对接方调用方式不变。

---

## 错误处理

错误统一为 **HTTP 状态码 + JSON `{"detail": "中文原因"}`**（合成类接口）：

| 状态码 | 场景 | 对接方建议 |
| --- | --- | --- |
| 400 | 文本为空 / 超 1000 字 / 角色名含非法字符 / 请求体不是 JSON | 按 `detail` 提示，超长文本分段后再试 |
| 404 | 角色 ID 不存在或未就绪 | 重新 `GET /models` 确认可用角色 |
| 405 | 用错了 HTTP 方法（如 GET 当 POST） | 检查请求方法 |
| 500 | 合成失败（引擎异常等） | 稍后重试；持续失败联系服务方看日志 |
| 连接拒绝 / 超时 | 服务未启动或未就绪 | 提示"语音服务未启动"，稍后重试 |

---

## 注意事项（对接前必读）

> 角色切换机制、耗时实测、并发排队等行为说明见上一节「角色切换与耗时」，此处只列其余要点。

1. **文本上限 1000 字**（`max_chars` 字段可查）：更长的文本请按句子切成多段分别合成。文本里的链接、代码、emoji 等会被服务自动清洗掉。
2. **不要缓存角色列表**：角色目录热加载，列表随时可能变；每次进入设置页时现查 `/models` 即可。
3. **返回的是二进制**：配音接口的响应体是音频文件本身，别按 JSON 解析；出错时（状态码非 2xx）响应体才是 JSON。
4. **跨域**：已放开 CORS（`Access-Control-Allow-Origin: *`），网页前端可直接从任意页面调用。
5. **局域网访问**：服务监听 `0.0.0.0`，同一局域网内其他机器用 `http://<服务机IP>:18062` 即可调用；跨网段需服务方自行开放防火墙端口。

## 附：完整接口清单（本文档只覆盖上面对外三接口，其余为服务维护用）

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/health` | GET | ① 查服务状态 |
| `/models`、`/v1/audio/voices`、`/v1/models` | GET | ② 查角色列表 |
| `/v1/audio/speech`、`/tts` | POST | ③ 配音（JSON→mp3 / 表单→wav） |
| `/` | GET | 网页试听界面 |
| `/api/free_memory`、`/api/shutdown`、`/api/cache_*`、`/api/delete_role` | POST/GET | 服务方维护用，对接方勿调（`/api/shutdown` 会彻底关闭服务且不自动重启） |
