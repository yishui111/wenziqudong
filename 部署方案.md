# 部署方案（wenziqudong 文字驱动语音）

> 换电脑复原依据；**每次优化/修复后必须同步更新本文件**（详细步骤见 [DEPLOY.md](DEPLOY.md)）。
> 仓库只含自研代码/测试/文档；引擎（GPT-SoVITS）、Python 运行时、音色模型等**大件不随仓库分发**，
> 获取与摆放方式见 DEPLOY.md 第 4 节「资源下载与摆放」。

## 1. 项目简介

文字驱动语音服务（独立项目，端口固定 18062）：输入文字 → 用训练好的 GPT-SoVITS 音色模型合成语音（wav/mp3）。
只做推理；音色模型（4 件套：`<角色名>.ckpt` / `<角色名>.pth` / `ref.wav` / `ref_text.txt`）复制到
`tts_service\models\<角色名>\` 目录即自动识别（目录扫描、热刷新，无需重启）。

## 2. 新电脑要求

- Windows 10/11（一键脚本为 .bat；也可用系统 Python 直接运行，见 DEPLOY.md 6.2）
- Python 3.10–3.12（推荐 3.12）；NVIDIA 显卡推理建议 ≥6GB 显存（无 GPU 可 `TTS_DEVICE=cpu`）
- 磁盘：引擎 + 运行时 + 音色模型约 15GB（全部自备下载/拷贝）

## 3. 目录结构

```
wenziqudong\
├─ tts_service\tts_api.py         主服务（FastAPI，18062）
│   ├─ tts_run.ps1                 启动窗口（把服务拉起一次，绝不自动重启；写 pid/日志到 tmp\）
│   ├─ models\<角色名>\            音色模型 4 件套（自备，自动识别）
│   └─ tmp\                       合成临时输出 + 日志/pid（运行时生成，不入库）
├─ gptsovits\GPT-SoVITS\          第三方推理引擎（官方仓库，自行下载）
├─ runtime\                       内置式 Python 3.12 + ffmpeg（自行准备）
├─ tests\                         自研测试脚本
├─ start.bat / stop.bat           通用一键启停（等价原版中文名脚本）
├─ 一键启动文字驱动语音.bat / 关闭文字驱动语音.bat
├─ README.md / DEPLOY.md / 部署方案.md / AGENTS.md
```

## 4. 启动/停止

- 启动：双击 `start.bat` 或 `一键启动文字驱动语音.bat` → http://127.0.0.1:18062/
  - **可反复双击**：已运行 → 提示退出；不会产生第二个服务。
  - **没有任何自动启动/自动重启**：服务退出后启动窗口只打印退出原因；再次启动必须重新双击 start.bat。启动失败（如显卡/内存被占）直接报错退出，等资源空出来再启动。
  - 首次加载 torch/模型约 **5–10 分钟**，就绪后自动打开浏览器。
- 停止：双击 `stop.bat` 或 `关闭文字驱动语音.bat` —— 按路径清扫本项目全部相关进程并**验证端口已释放**；**关闭后绝不自动重启**。
  也可直接**关闭服务窗口（黑框）**：python 由 Windows Job 对象托管，窗口一死内核立即终止服务，显卡/内存随之释放；网页也有「⏹ 关闭服务」按钮。
- 端口：**固定 18062**（对外接口地址必须稳定，不自动漂移）。18062 在 Windows 动态端口范围（1024~15000）之外，不会被系统临时连接随机抢占；同机 duihuamoxing 用 8061/18060，完全错开。
  若 18062 被其他程序占用，start.bat 直接报错退出、不会自动换端口；仅当 18062 确实不可用时才临时 `set TTS_API_PORT=8070`。清理/停止只针对本项目副本，不影响其他项目的同名服务。
- 设备：默认 `cuda`（NVIDIA 显卡加速）；不想占显卡可先 `set TTS_DEVICE=cpu` 再启动（合成稍慢）。

## 5. 接口

> 📄 对外对接文档在项目根目录 [接口文档.md](接口文档.md)（面向调用方：查状态 / 查角色 / 配音三接口 + 示例 + 错误处理），本表为速查。

| 接口 | 方法 | 说明 |
|---|---|---|
| `/` | GET | 网页界面（输入文字→合成语音；自动试听/下载/历史/释放显存/关闭服务） |
| `/tts` | POST | `text`+`character`+可选 `speed/top_k/top_p/temperature/sample_steps` → wav（内存直出，不落盘） |
| `/models` | GET | 角色列表（含 ready 状态，热刷新） |
| `/health` | GET | 健康检查（含 max_chars 字数上限） |
| `/v1/audio/speech` | POST | **OpenAI 兼容 TTS**：JSON `{model, input, voice, speed}` → mp3（voice=角色名；`input`/`voice` 也接受 `text`/`character` 写法） |
| `/v1/audio/voices` | GET | OpenAI 兼容音色列表 |
| `/v1/models` | GET | OpenAI 兼容角色模型列表 |
| `/api/delete_role` | POST | 删除角色（真删本地模型目录，防误删双确认） |
| `/api/cache_mode` | GET/POST | 缓存策略：默认单角色（切换自动释放上一个，省显存）；`multi=1` 多角色常驻 |
| `/api/cache_status` | GET | 查看显存中已缓存的角色模型 |
| `/api/free_memory` | POST | 清空角色模型缓存并释放显存 |
| `/api/shutdown` | POST | 彻底关闭服务（退出进程，绝不自动重启） |

```bash
curl -X POST -F "text=大家好，我是雷军。" -F "character=liejun" http://127.0.0.1:18062/tts -o out.wav
```

## 6. 声音模型约定（关键）

每个角色目录必须 4 件齐全才可用：`<角色名>.ckpt`（GPT 权重）、`<角色名>.pth`（SoVITS 权重）、
`ref.wav`（该角色 3–10 秒干净人声）、`ref_text.txt`（与音频一字不差的文字，UTF-8）。
缺文件的角色显示「缺文件」，不会出现在合成下拉框；`ref.wav` 超长或 ref_text 与音频不符会明显漏字/跳读。

## 7. 复原步骤（换电脑）

1. 克隆/下载本仓库代码
2. 按 DEPLOY.md 第 4 节准备：引擎 `gptsovits\GPT-SoVITS`、运行时 `runtime\py312`、`runtime\ffmpeg`、NLTK 资源
3. 把音色模型目录复制到 `tts_service\models\`
4. 双击 `start.bat`，浏览器打开 http://127.0.0.1:18062/
5. 选角色、输文字、合成，确认 wav 可播放

## 8. 验证

- `GET /health` 正常；`GET /models` 列出角色
- `python tests\test_tts_client.py --character <角色名>` 合成成功、音频可播放且音色像该角色
- `python tests\test_split_text.py` 分句/清洗逻辑回归（不依赖服务，不加载引擎）
- 测试产物放 `tests\`（音频不入库）

## 9. 已知问题 / 注意事项

- 合成时 GPU 显存约 3–5GB，与其它大显存任务同机跑注意错开。
- 文字超过上限（默认 1000 字，`TTS_MAX_CHARS` 可调）会拒绝，请分段合成。
- 每个角色模型约 230MB、首次加载 1–2 分钟，之后常驻缓存秒回；服务重启缓存清空属正常。
  多个音色频繁切换可勾选「缓存多个角色模型」。
- 合成前自动清洗 markdown/URL/特殊符号，长回复也能正常朗读。
- 页面「Failed to fetch」= 连不上服务：正在启动（等 5–10 分钟）或已停止（重新双击启动脚本）。
- 本仓库公开后**不包含**任何真人音色模型/训练素材；请勿向公开仓库上传他人声音克隆产物。
